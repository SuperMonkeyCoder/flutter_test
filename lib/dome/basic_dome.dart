import 'package:flutter/material.dart';

class BasicDome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.person,
      color: Colors.yellow,
      size: 40,
    );
  }
}
